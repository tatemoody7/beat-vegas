name: Daily grading (results + ledger)

# Refreshes final scores from CFBD and grades the market/model/pick ledgers,
# writing to Neon. Runs in the cloud because the local Mac's usual network
# can't reach Neon (port 5432 filtered). Every morning (since 2026-09-08): a
# Thursday game is graded Friday morning, Friday's on Saturday, Saturday's on
# Sunday, and Monday's run is the full weekly pass. grade.py is season-wide and
# delete-then-rewrite idempotent, so a late final simply grades next morning. Off-season every step is a green no-op.
# Each run is kept CHEAP (and since 2026-09-22 a run that finds a completed
# grading run inside the last four hours skips the work -- see the probe): the
# finals refresh only writes games CFBD marks completed (a noon-ET Saturday run
# never freezes a live score), the PBP step fetches only the weeks still missing
# fh_team_game rows, and the post-mortem regrades only the live season except on
# Monday (ET) -- or a dispatch with hist=true -- when it also redoes the 2023-25
# history. (Until 2026-09-16 EVERY dispatch did the history pass, and the Vercel
# cron that is this workflow's primary trigger IS a dispatch, so the 7 MB read +
# 3.5 MB rewrite ran daily instead of weekly.)
# Post-game refresh + grading: finals, 1H play-by-play aggregates, then the
# ledgers. Pre-game enrichment (pace/weather) + scoring run Sundays
# (sunday.yml). A red run here means "the ledger or PBP factors are stale";
# GitHub emails it and the Monday coaching routine re-kicks the workflow.
# No ODDS_API_KEY: grading reads snapshots already in Neon.

# Least privilege: these jobs read the repo and write to Neon via
# secrets; none of them pushes to git. Without this block the job
# inherits whatever the repository default happens to be, and that
# default is a setting someone can change later without touching
# any workflow.
permissions:
  contents: read

on:
  schedule:
    # 10:30 UTC daily = 6:30am EDT / 5:30am EST, well ahead of card.yml's
    # 12:05Z morning build in the shared neon-writers group (GitHub cancels the
    # older PENDING run when a third arrives). 16:00Z is the retry / late-finals
    # pass. GitHub cron is best-effort; grading spends no Odds API credits.
    - cron: '30 10 * * *'
    - cron: '0 16 * * *'
  workflow_dispatch:
    inputs:
      hist:
        description: 'also regrade the 2023-25 history (post-mortem scope both); default is Monday ET only'
        type: boolean
        required: false
        default: false

concurrency:
  group: neon-writers
  cancel-in-progress: false

jobs:
  grade:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements/lock.txt
      - name: Install
        # Hash-pinned: requirements/lock.txt names the exact release AND sha256 of
        # every package (see its header), so a compromised upload of any of them
        # cannot reach a runner that holds DATABASE_URL and the API keys. The
        # editable install of this repo then runs without build isolation, so
        # the build backend (setuptools, also in the lock) is never fetched
        # unhashed. Local dev keeps plain `pip install -e .`.
        run: |
          pip install --require-hashes -r requirements/lock.txt
          pip install --no-deps --no-build-isolation -e .
      - name: Restore CFBD reference data (weekly cache)
        # grade_factor_ledger.py builds the feature frame, which fetches the
        # SP+/talent/roster/returning references (~22 CFBD calls) -- twice a
        # day here. This job was the one heavy caller NOT sharing the cache.
        id: cfbd_cache
        uses: ./.github/actions/cfbd-cache
      - name: Resolve season
        id: season
        run: |
          echo "season=$(python -c 'from beatvegas.season import current_season; print(current_season())')" >> "$GITHUB_OUTPUT"
          # ISO weekday in Eastern time (1 = Monday): the weekly full pass.
          echo "et_dow=$(TZ=America/New_York date +%u)" >> "$GITHUB_OUTPUT"
      - name: Already graded in the last four hours? (the minutes guard)
        id: probe
        env:
          IN_HIST: ${{ inputs.hist }}
        # Three triggers fire every day -- the Vercel cron's dispatch (~10:52Z)
        # and GitHub's two crons (10:30Z and 16:00Z), which run 2-4 hours late
        # -- and until 2026-09-22 all three ran every step: ~18 runner-minutes a
        # day for one useful pass, measured over 09-20/09-21. Same idea as
        # card.yml's `cards`-row probe and sunday.yml's `captured` step: a
        # POSITIVE fact from the database, not a run-list lookup. The fact here
        # is a `postmortem_runs` row with scope=live -- the post-mortem is this
        # job's LAST step, so its row proves the whole run completed. A
        # four-hour window (not "today") keeps the deliberate second daily pass
        # for late finals while collapsing the 10:30Z-cron / 10:52Z-dispatch
        # pair and any late duplicate. `hist=true` (the manual history regrade)
        # always runs.
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import os
          from datetime import datetime, timedelta
          from sqlalchemy import text
          from beatvegas.db.store import init_db, session_scope
          init_db()
          # computed_at is stored naive UTC.
          since = datetime.utcnow() - timedelta(hours=4)
          with session_scope() as s:
              recent = s.execute(
                  text(
                      "SELECT count(*) FROM postmortem_runs "
                      "WHERE scope = 'live' AND computed_at >= :since"
                  ),
                  {"since": since},
              ).scalar() or 0
          force = os.environ.get("IN_HIST") == "true"
          print(f"need_grade={'true' if (force or recent == 0) else 'false'}")
          print(f"recent_runs={recent}")
          PY
          cat "$GITHUB_OUTPUT"
      - name: Refresh schedule, lines and scores (CFBD)
        if: steps.probe.outputs.need_grade == 'true'
        # Primary source: this is also what creates new games and refreshes
        # lines, so it runs first. continue-on-error because ESPN below covers
        # the part grading actually needs -- on 2026-09-12 CFBD hit its monthly
        # call quota and 429'd for two days, taking every grade with it.
        continue-on-error: true
        env:
          SEASON: ${{ steps.season.outputs.season }}
        run: python scripts/backfill.py --season "$SEASON"
      - name: Backfill any missing final scores (ESPN, no key, no quota)
        if: steps.probe.outputs.need_grade == 'true'
        # Scores only, onto games the step above already created, keyed by id
        # (ESPN's event ids are the ids we store). Second source for the one
        # input grading cannot do without. Four days back: this runs twice a
        # day, so a Saturday final is caught Sunday, Monday and Tuesday; ten
        # days was eleven scoreboard calls a run for a window nothing late
        # ever landed in.
        continue-on-error: true
        run: python scripts/backfill_scores_espn.py --days-back 4
      - name: Refresh 1H play-by-play aggregates (CFBD /plays, finished games)
        if: steps.probe.outputs.need_grade == 'true'
        # fh_team_game feeds the season-to-date FH factors the Sunday scorer
        # reads; without this the current season never accrues PBP rows.
        # --only-missing: one CFBD call per week that still has a finished FBS
        # game without rows (usually one, often none) instead of all 20 weeks
        # twice a day; upsert by (game_id, off_team), fail-silent per season.
        # Enrichment, not grading: PBP aggregates feed next week's SCORING, so a
        # flaky CFBD call here must never stop this week's grades from landing.
        continue-on-error: true
        env:
          SEASON: ${{ steps.season.outputs.season }}
        run: python scripts/backfill_pbp.py --season "$SEASON" --only-missing
      - name: Grade market vs model ledger
        if: steps.probe.outputs.need_grade == 'true'
        env:
          SEASON: ${{ steps.season.outputs.season }}
        run: python scripts/grade.py --season "$SEASON"
      - name: Grade my picks
        if: steps.probe.outputs.need_grade == 'true'
        env:
          SEASON: ${{ steps.season.outputs.season }}
        run: python scripts/pick.py grade --season "$SEASON"
      - name: Grade frozen game records
        if: steps.probe.outputs.need_grade == 'true'
        run: python scripts/grade_records.py
      - name: Grade factor credibility ledger (real lines only)
        if: steps.probe.outputs.need_grade == 'true'
        # Display chips only -- nothing downstream reads this to decide a bet,
        # and it calls CFBD /ratings/sp, which 429'd and skipped the post-mortem
        # on 2026-09-13 after this guard was reverted by mistake.
        continue-on-error: true
        run: python scripts/grade_factor_ledger.py
      - name: Post-mortem (rated games vs outcomes -> postmortem_* tables)
        if: steps.probe.outputs.need_grade == 'true'
        # Regrades the season's cards at Hard Rock's numbers (scope live) every
        # run; the stored 2023-25 ratings under the flat and fair proxies (scope
        # hist, the expensive half) only on Monday ET or when dispatched with
        # hist=true. NOT on every dispatch: the Vercel cron dispatches this
        # workflow daily, so keying on the event ran the history pass every day.
        # Delete-then-rewrite per scope, no API calls. Feeds the Results page
        # panel and docs/POST_MORTEM.md.
        env:
          ET_DOW: ${{ steps.season.outputs.et_dow }}
          IN_HIST: ${{ inputs.hist }}
        run: |
          scope=live
          if [ "$ET_DOW" = "1" ] || [ "$IN_HIST" = "true" ]; then scope=both; fi
          echo "post-mortem scope=$scope (et_dow=$ET_DOW hist=$IN_HIST event=$GITHUB_EVENT_NAME)"
          python scripts/post_mortem.py --write --md "" --scope "$scope"
      - name: Save CFBD reference cache (only when this run populated it)
        if: always() && steps.cfbd_cache.outputs.hit != 'true'
        uses: ./.github/actions/cfbd-cache-save
        with:
          key: ${{ steps.cfbd_cache.outputs.key }}
